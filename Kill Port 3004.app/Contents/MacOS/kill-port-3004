#!/bin/bash

# Kill processes on port 3004
echo "Killing processes on port 3004..."

# Find and kill processes using port 3004
PIDS=$(lsof -ti:3004 2>/dev/null)

if [ -n "$PIDS" ]; then
    echo "Found processes: $PIDS"
    echo "$PIDS" | xargs kill -9 2>/dev/null
    echo "Processes on port 3004 have been terminated."
    osascript -e 'display notification "Port 3004 processes killed ✅" with title "Bitcoin Email" sound name "Glass"'
else
    echo "No processes found on port 3004."
    osascript -e 'display notification "No processes found on port 3004" with title "Bitcoin Email" sound name "Tink"'
fi